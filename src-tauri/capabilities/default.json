{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the AIPet window.",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
